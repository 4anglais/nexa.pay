# NexaPayslip Deployment Guide

## Environment Variables Setup

### Local Development

Your `.env` file should contain the necessary Firebase credentials. These environment variables use the `VITE_` prefix to ensure they're available in the browser during build time.
